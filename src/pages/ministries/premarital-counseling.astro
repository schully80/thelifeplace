---
import BaseLayout from "../../layouts/BaseLayout.astro";
import RegisterForm from "../../components/RegisterForm.astro";
---

<BaseLayout title="Premarital Counseling">
  <section class="container-x py-12 md:py-16 text-center">
    <div class="rounded-xl border bg-white p-6 shadow-sm ring-1 ring-orange-200">
      <div class="flex items-center justify-between mb-4">
        <h1 class="text-3xl font-medium">Premarital Counseling</h1>
        <span class="inline-block rounded-full bg-orange-500 px-3 py-1 text-sm font-semibold text-white">
          Launching Soon
        </span>
      </div>
      <p class="text-lg text-gray-700">
        We’re preparing resources to help couples build Christ-centred marriages. Stay tuned as we launch soon at The Life Place.</br> 
        <em class="text-[#B3282D]">Come. See. Jesus.</em>
      </p>
    </div>
     <!-- Registration Form -->
    <div class="mt-12 max-w-2xl mx-auto">
      <RegisterForm
        title="We'd love to see you — drop your details below"
        titleClass="text-2xl font-medium text-brand-warmgray-800 text-center mb-4"
        eventName="Bring Them To Jesus"
        paymentLink="/give"
        successMessage="Thank you for registering!"
        ctaText="Save My Spot"
        disabled={true}
        fields={[
          { id: "firstName", label: "Name", type: "text", required: true },
          { id: "surname", label: "Surname", type: "text", required: true },
          { id: "contact", label: "Contact Number", type: "tel", required: true },
          { id: "email", label: "Email Address", type: "email", required: true },
          { id: "streetAddress", label: "Street Address", type: "text", required: true },
          { id: "suburb", label: "Suburb", type: "text", required: true },
          { id: "city", label: "City", type: "text", required: true },
          { id: "postalCode", label: "Postal Code", type: "text", required: true, minlength: 3, maxlength: 10, pattern: ".{3,10}" },
          { id: "attendees", label: "Number of People Attending", type: "number", required: true, min: 1 }
        ]}

             <p class="text-lg text-brand-warmgray-700">
  {thankYouMessage}
</p>
<div>
  </section>
</BaseLayout>