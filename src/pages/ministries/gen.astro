---
import BaseLayout from "../../layouts/BaseLayout.astro";
import RegisterForm from "../../components/RegisterForm.astro";
---
<BaseLayout title="Gen">
  <!-- Full-bleed hero with dark overlay -->
  <section class="relative full-bleed">
    <img
      src="/youth-gen.jpg"
      alt="Youth at The Life Place"
      class="absolute inset-0 h-full w-full object-cover"
      loading="eager"
      decoding="async"
      fetchpriority="high"
    />
    <div class="absolute inset-0 bg-black/20"></div>

    <!-- Centered hero text -->
    <div class="relative z-10 flex items-center justify-center min-h-[50vh] md:min-h-[60vh] px-4">
      <!-- Mobile: slightly smaller; Desktop unchanged -->
      <h1 class="text-4xl md:text-6xl font-extrabold italic text-white drop-shadow-lg">
        ThisGen
      </h1>
    </div>
  </section>

  <!-- Content section -->
  <section class="max-w-5xl mx-auto px-4 py-12 md:py-20">
    <div class="rounded-xl border bg-white p-5 md:p-6 shadow-sm ring-1 ring-blue-200">
      <!-- Mobile: text-base; Desktop: text-xl -->
      <p class="text-warmgray-800 text-lg md:text-xl font-medium leading-7 md:leading-relaxed">
        <strong class="text-brand-red">ThisGen</strong> is our youth expression at The Life Place — 
        a space for our generation to <span class="text-brand-red font-medium">Come. See. Jesus</span>, 
        grow in faith, build lifelong friendships, and discover their purpose — 
        all for the glory of Jesus and the good of the world.
      </p>
    </div>

    <!-- Registration Form -->
    <div class="mt-10 md:mt-12 max-w-2xl mx-auto px-4 md:px-0">
      <RegisterForm
        title="We'd love to see you — drop your details below"
        titleClass="text-xl md:text-2xl font-medium text-brand-warmgray-800 text-center mb-4"
        eventName="ThisGen"
        paymentLink="/give"
        successMessage="Thank you for registering!"
        ctaText="Save My Spot"
        disabled={true}
         showNotice={true}
        fields={[
          { id: "firstName", label: "Name", type: "text", required: true },
          { id: "surname", label: "Surname", type: "text", required: true },
          { id: "contact", label: "Contact Number", type: "tel", required: true },
          { id: "email", label: "Email Address", type: "email", required: true },
          { id: "streetAddress", label: "Street Address", type: "text", required: true },
          { id: "suburb", label: "Suburb", type: "text", required: true },
          { id: "city", label: "City", type: "text", required: true },
          { id: "postalCode", label: "Postal Code", type: "text", required: true, minlength: 3, maxlength: 10, pattern: ".{3,10}" },
          { id: "attendees", label: "Number of People Attending", type: "number", required: true, min: 1 }
        ]}
      />
    </div>
  </section>
</BaseLayout>
