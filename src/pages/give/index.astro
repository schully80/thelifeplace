---
import BaseLayout from "../../layouts/BaseLayout.astro";
import QuickGive from "../../components/giving/QuickGive.astro";
import { site } from "../../../site.config.js";
const event = new URL(Astro.request.url).searchParams.get("event");
const { bank, snapscan, paypal, westernUnion } = site.giving;
const supportEmail = site.contact.email;
const supportAddress = site.contact.address;
const supportPhoneDisplay = "+27 76 563 9460";
const supportPhoneHref = "tel:+27765639460";
---
<BaseLayout title="Generosity — The Life Place">

<!-- FULL-WIDTH HERO SECTION (Single Image) -->
<section class="relative w-screen h-[35vh] md:h-[40vh] lg:h-[65vh] overflow-hidden bg-black -ml-[calc((100vw-100%)/2)]">
  <img
    src="/Online giving photo smaller font.jpg"
    alt="Generosity expressed in community"
    class="absolute inset-0 w-full h-full object-cover object-center rounded-none hero-single-img"
    loading="eager"
    decoding="async"
    fetchpriority="high"
  />
  <div class="absolute inset-0 bg-gradient-to-b from-black/25 via-black/10 to-black/25"></div>
</section>

<!-- PAGE CONTENT CONTAINER -->
<div class="w-full max-w-6xl xl:max-w-7xl 2xl:max-w-[88rem] mx-auto px-4 sm:px-6 lg:px-8">

<!-- PAGE HEADER -->
<section class="w-full px-0 py-8 md:py-8 lg:py-8 bg-white relative">
  <div class="max-w-6xl mx-auto px-6 py-12 md:py-28 lg:py-32 text-center">

    <h1 class="block text-4xl md:text-5xl lg:text-7xl font-black text-gray-800 leading-tight md:leading-tight lg:leading-tight">
      Extravagant Generosity
    </h1>

    <p
      class="mt-8 text-xl md:text-xl lg:text-2xl
             text-gray-600 leading-relaxed
             max-w-4xl mx-auto"
    >
      Generosity is our expression of who <span class="text-brand-red">Jesus</span> is and what He does.
    </p>

  </div>
</section>

<!-- IMPACT STATEMENT -->
<section class="w-full px-4 md:px-6 lg:px-8 py-6 md:py-6 lg:py-8">
  <div class="max-w-4xl mx-auto">
    <h2 class="block heading-2 text-center">
      Your generosity creates more opportunities and resources so others can
      <span class="block text-brand-red">
        Come. See. Jesus
      </span>
    </h2>
  </div>
</section>

<!-- QUICK GIVE + METHODS (Unified Container) -->
<section class="w-full px-4 md:px-6 lg:px-8 py-8 md:py-20 lg:py-24 relative">
  <div class="max-w-6xl mx-auto">

    <!-- Guide Text (Above Container) -->
    <p class="text-xl md:text-xl lg:text-2xl text-gray-600 text-center mb-10 md:mb-16 md:max-w-full mx-auto leading-relaxed">
      Below is a simple, guided way to give generously.
    </p>

    <!-- Unified Container: QuickGive + Methods -->
    <div class="rounded-[32px] bg-white/80 backdrop-blur-xl border border-black/10 shadow-sm hover:shadow-md transition overflow-hidden">
      
      <!-- QuickGive Section (Top) -->
      <div class="p-4 sm:p-6 md:p-14 lg:p-20 pb-4 md:pb-8 lg:pb-10">
        <QuickGive />
      </div>

      <!-- Methods Section (Bottom) -->
      <div id="giving-methods" class="pt-8 md:pt-8 lg:pt-10 p-6 md:p-10 lg:p-14 xl:p-16">
        <div class="rounded-[32px] border border-black/10 bg-gradient-to-br from-stone-50 via-white to-red-50 p-5 md:p-8 lg:p-10">
          <div class="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between">
            <div class="max-w-3xl">
              <p class="text-lg md:text-2xl font-bold text-gray-800">Step 3</p>
              <h2 class="mt-4 text-2xl md:text-3xl lg:text-4xl font-black text-gray-800 uppercase">
                Choose Your Giving Method
              </h2>
              <p class="mt-4 text-base md:text-lg text-gray-600 leading-relaxed">
                Choose the option that fits where you are and how you prefer to give.
              </p>
            </div>

            <div class="rounded-[28px] border border-black/10 bg-white/85 p-4 md:p-5 shadow-sm lg:max-w-md">
              <div class="flex items-start justify-between gap-4">
                <div>
                  <p class="text-xs font-bold uppercase tracking-[0.18em] text-gray-500">Secure Giving</p>
                  <p class="mt-2 text-sm md:text-base text-gray-600 leading-relaxed">
                    We do not store your card or banking details. Every online option below completes on the payment provider’s side.
                  </p>
                </div>
                <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-2xl bg-brand-red text-white">
                  <i class="fa-solid fa-shield-halved text-base" aria-hidden="true"></i>
                </div>
              </div>
            </div>
          </div>

          <div class="mt-8 grid grid-cols-1 gap-5 md:grid-cols-2 md:gap-6">
            <!-- EFT -->
            <article
              class="fadeIn relative overflow-hidden rounded-[30px] border border-black/10 bg-white p-6 md:p-8 shadow-sm transition-all duration-300 hover:-translate-y-1 hover:shadow-lg"
              style="animation-delay: 0ms"
            >
              <div class="pointer-events-none absolute -right-10 -top-10 h-28 w-28 rounded-full bg-red-100 blur-2xl"></div>
              <div class="relative flex h-full flex-col">
                <div class="flex items-start justify-between gap-4">
                  <div class="flex flex-wrap gap-2">
                    <span class="inline-flex items-center rounded-full border border-black/10 bg-black/[0.03] px-3 py-1 text-xs font-semibold text-gray-700">
                      South Africa
                    </span>
                    <span class="inline-flex items-center rounded-full border border-black/10 bg-black/[0.03] px-3 py-1 text-xs font-semibold text-gray-700">
                      Bank Transfer
                    </span>
                  </div>
                  <div class="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-brand-red text-white shadow-sm">
                    <i class="fa-solid fa-laptop text-lg" aria-hidden="true"></i>
                  </div>
                </div>

                <div class="mt-4 text-center">
                  <h3 class="text-2xl font-black text-gray-800">EFT</h3>
                </div>

                <dl class="mt-6 grid grid-cols-1 gap-3 sm:grid-cols-2">
                  <div class="rounded-2xl border border-black/10 bg-black/[0.02] p-4">
                    <dt class="text-xs font-bold uppercase tracking-[0.14em] text-gray-500">Bank</dt>
                    <dd class="mt-2 text-sm md:text-base font-semibold text-gray-800">{bank.bankName}</dd>
                  </div>
                  <div class="rounded-2xl border border-black/10 bg-black/[0.02] p-4">
                    <dt class="text-xs font-bold uppercase tracking-[0.14em] text-gray-500">Account name</dt>
                    <dd class="mt-2 text-sm md:text-base font-semibold text-gray-800">{bank.accountName}</dd>
                  </div>
                  <div class="rounded-2xl border border-black/10 bg-black/[0.02] p-4">
                    <dt class="text-xs font-bold uppercase tracking-[0.14em] text-gray-500">Account number</dt>
                    <dd class="mt-2 text-sm md:text-base font-semibold text-gray-800">{bank.accountNumber}</dd>
                  </div>
                  <div class="rounded-2xl border border-black/10 bg-black/[0.02] p-4">
                    <dt class="text-xs font-bold uppercase tracking-[0.14em] text-gray-500">Branch code</dt>
                    <dd class="mt-2 text-sm md:text-base font-semibold text-gray-800">{bank.branchCode}</dd>
                  </div>
                  <div class="rounded-2xl border border-black/10 bg-black/[0.02] p-4 sm:col-span-2">
                    <dt class="text-xs font-bold uppercase tracking-[0.14em] text-gray-500">SWIFT</dt>
                    <dd class="mt-2 text-sm md:text-base font-semibold text-gray-800">{bank.swift}</dd>
                  </div>
                </dl>

                <p class="mt-4 text-sm text-gray-500 leading-relaxed">
                  Use these details in your banking app or when making a direct transfer from your bank.
                </p>
              </div>
            </article>

            <!-- SNAPSCAN -->
            <article
              class="fadeIn relative overflow-hidden rounded-[30px] border border-black/10 bg-white p-6 md:p-8 shadow-sm transition-all duration-300 hover:-translate-y-1 hover:shadow-lg"
              style="animation-delay: 100ms"
            >
              <div class="pointer-events-none absolute -right-10 -top-10 h-28 w-28 rounded-full bg-red-100 blur-2xl"></div>
              <div class="relative flex h-full flex-col">
                <div class="flex items-start justify-between gap-4">
                  <div class="flex flex-wrap gap-2">
                    <span class="inline-flex items-center rounded-full border border-black/10 bg-black/[0.03] px-3 py-1 text-xs font-semibold text-gray-700">
                      South Africa
                    </span>
                    <span class="inline-flex items-center rounded-full border border-black/10 bg-black/[0.03] px-3 py-1 text-xs font-semibold text-gray-700">
                      Fast Mobile Checkout
                    </span>
                  </div>
                  <div class="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-brand-red text-white shadow-sm">
                    <i class="fa-solid fa-mobile-screen-button text-lg" aria-hidden="true"></i>
                  </div>
                </div>

                <div class="mt-4 text-center">
                  <h3 class="text-2xl font-black text-gray-800">SnapScan</h3>
                  <p class="mt-3 text-sm text-gray-500">Scan the QR or tap through to give.</p>
                </div>

                <div class="mt-6 flex justify-center">
                  <div class="inline-flex rounded-[24px] border border-black/10 bg-black/[0.02] p-5">
                    <img
                      src={snapscan.qrImagePath}
                      alt="SnapScan QR"
                      class="h-32 w-32 object-contain md:h-36 md:w-36"
                    />
                  </div>
                </div>

                <div class="mt-6 flex justify-center">
                  <a
                    href={snapscan.url}
                    class="inline-flex items-center justify-center rounded-2xl bg-brand-red px-5 py-3 text-base font-bold text-white shadow-sm transition-colors duration-300 hover:bg-[#9f2429]"
                  >
                    Open SnapScan
                  </a>
                </div>
              </div>
            </article>

            <!-- PAYPAL -->
            <article
              class="fadeIn relative self-start overflow-hidden rounded-[30px] border border-black/10 bg-white p-6 md:p-8 shadow-sm transition-all duration-300 hover:-translate-y-1 hover:shadow-lg"
              style="animation-delay: 200ms"
            >
              <div class="pointer-events-none absolute -right-10 -top-10 h-28 w-28 rounded-full bg-red-100 blur-2xl"></div>
              <div class="relative flex h-full flex-col">
                <div class="flex items-start justify-between gap-4">
                  <div class="flex flex-wrap gap-2">
                    <span class="inline-flex items-center rounded-full border border-black/10 bg-black/[0.03] px-3 py-1 text-xs font-semibold text-gray-700">
                      International Giving
                    </span>
                  </div>
                  <div class="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-brand-red text-white shadow-sm">
                    <i class="fa-brands fa-paypal text-lg" aria-hidden="true"></i>
                  </div>
                </div>

                <div class="mt-4 text-center">
                  <h3 class="text-2xl font-black text-gray-800">PayPal</h3>
                  <p class="mt-3 text-sm text-gray-500">Give securely online via PayPal.</p>
                </div>

                <div class="mt-6 flex justify-center">
                  <img
                    src="https://www.paypalobjects.com/en_US/i/btn/btn_donateCC_LG.gif"
                    alt=""
                    class="rounded-2xl border p-2 bg-white shadow-sm"
                  />
                </div>

                <form action="https://www.paypal.com/donate" method="post" target="_top" class="mt-6 flex justify-center">
                  <input type="hidden" name="hosted_button_id" value={paypal.hostedButtonId} />
                  <button
                    type="submit"
                    class="inline-flex items-center justify-center rounded-2xl bg-brand-red px-5 py-3 text-base font-bold text-white shadow-sm transition-colors duration-300 hover:bg-[#9f2429]"
                  >
                    Give via PayPal
                  </button>
                </form>
              </div>
            </article>

            <!-- WESTERN UNION -->
            <article
              class="fadeIn relative overflow-hidden rounded-[30px] border border-black/10 bg-white p-6 md:p-8 shadow-sm transition-all duration-300 hover:-translate-y-1 hover:shadow-lg"
              style="animation-delay: 300ms"
            >
              <div class="pointer-events-none absolute -right-10 -top-10 h-28 w-28 rounded-full bg-red-100 blur-2xl"></div>
              <div class="relative flex h-full flex-col">
                <div class="flex items-start justify-between gap-4">
                  <div class="flex flex-wrap gap-2">
                    <span class="inline-flex items-center rounded-full border border-black/10 bg-black/[0.03] px-3 py-1 text-xs font-semibold text-gray-700">
                      International Transfer
                    </span>
                  </div>
                  <div class="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-brand-red text-white shadow-sm">
                    <span class="text-sm font-black tracking-tight" aria-hidden="true">WU</span>
                  </div>
                </div>

                <div class="mt-4 text-center">
                  <h3 class="text-2xl font-black text-gray-800">Western Union</h3>
                </div>

                <dl class="mt-6 grid grid-cols-1 gap-3">
                  <div class="rounded-2xl border border-black/10 bg-black/[0.02] p-4">
                    <dt class="text-xs font-bold uppercase tracking-[0.14em] text-gray-500">Recipient</dt>
                    <dd class="mt-2 text-sm md:text-base font-semibold text-gray-800">{site.name}</dd>
                  </div>
                  <div class="rounded-2xl border border-black/10 bg-black/[0.02] p-4">
                    <dt class="text-xs font-bold uppercase tracking-[0.14em] text-gray-500">Email</dt>
                    <dd class="mt-2 text-sm md:text-base font-semibold text-gray-800 break-all">
                      <a href={`mailto:${supportEmail}`} class="hover:text-brand-red transition">
                        {supportEmail}
                      </a>
                    </dd>
                  </div>
                  <div class="rounded-2xl border border-black/10 bg-black/[0.02] p-4">
                    <dt class="text-xs font-bold uppercase tracking-[0.14em] text-gray-500">Phone</dt>
                    <dd class="mt-2 text-sm md:text-base font-semibold text-gray-800">
                      <a href={supportPhoneHref} class="hover:text-brand-red transition">
                        {supportPhoneDisplay}
                      </a>
                    </dd>
                  </div>
                  <div class="rounded-2xl border border-black/10 bg-black/[0.02] p-4">
                    <dt class="text-xs font-bold uppercase tracking-[0.14em] text-gray-500">Address</dt>
                    <dd class="mt-2 text-sm md:text-base font-semibold text-gray-800 leading-relaxed">
                      {supportAddress.line1}<br />
                      {supportAddress.line2}<br />
                      {supportAddress.line3}<br />
                      {supportAddress.line4}
                    </dd>
                  </div>
                </dl>

                <div class="mt-6 flex justify-center">
                  <a
                    href={westernUnion.url}
                    target="_blank"
                    rel="noopener"
                    class="inline-flex items-center justify-center rounded-2xl bg-brand-red px-5 py-3 text-base font-bold text-white shadow-sm transition-colors duration-300 hover:bg-[#9f2429]"
                  >
                    {westernUnion.ctaLabel}
                  </a>
                </div>
              </div>
            </article>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- GIVE IN PERSON TEXT ONLY (outside container) -->
<section class="w-full px-4 md:px-6 lg:px-8 py-16 md:py-24 relative">
  <div class="max-w-3xl mx-auto text-center">
    <div class="flex justify-center mb-6">
      <i class="fa-solid fa-hand-holding-heart text-brand-red text-3xl md:text-4xl" aria-hidden="true"></i>
    </div>
    <p class="text-2xl md:text-3xl leading-relaxed text-gray-600">
      While we encourage online giving for security and simplicity, we gladly receive in-person gifts during our weekend gatherings.
    </p>
  </div>
</section>

 <!-- Annual Report (featured card) -->
<section class="w-full px-4 md:px-6 lg:px-8 py-16 md:py-24">
  <div class="max-w-6xl mx-auto">
    <div
      class="relative overflow-hidden rounded-3xl md:rounded-[36px] border bg-white shadow-sm hover:shadow-md hover:-translate-y-0.5 transform transition
             p-8 md:p-14"
      aria-labelledby="annual-report-title"
    >
    
    <div class="flex flex-col items-center gap-4">
      <!-- Icon -->
      <div class="flex h-12 w-12 items-center justify-center rounded-xl">
        <i class="fa-solid fa-file-lines text-brand-red text-3xl" aria-hidden="true"></i>
      </div>

      
      <div class="flex-1 w-full">
        <h2 id="annual-report-title" class="heading-2 text-center">
          Annual Financial Report
        </h2>
     
      <p
        class="mt-4 md:mt-5 text-base md:text-xl text-center
               leading-relaxed text-gray-600"
      >
        We value transparency and faithful stewardship.
        This report is shared with those who call
        <span class="font-semibold">The Life Place</span> home,
        reflecting how generosity is handled with care —
        to glorify <span class="text-brand-red">Jesus</span>, meet real needs, and serve the mission of
        The Life Place.
      </p>

<p class="mt-4 text-center text-base md:text-lg text-gray-600 leading-relaxed">
  To receive this report, contact us at
  <a href={`mailto:${supportEmail}`} class="font-semibold text-brand-red hover:underline">
    {supportEmail}
  </a>
  or during office hours at
  <a href={supportPhoneHref} class="font-semibold text-brand-red hover:underline">
    {supportPhoneDisplay}
  </a>
</p>

  </div>
</section>


<!-- Giving FAQ -->
<section class="w-full px-4 md:px-6 lg:px-8 py-16 md:py-24">
  <div class="max-w-6xl mx-auto data-faq-accordion">
  <!-- Header -->
  <div class="text-center mb-10 md:mb-14">
    <h2 class="heading-2">
      Giving FAQs
    </h2>
    <p class="mt-3 md:mt-4 text-base md:text-lg text-gray-600">
      Common questions about giving at The Life Place.
    </p>
  </div>

  <!-- FAQ Accordion -->
  <div class="space-y-4 md:space-y-5">

    <!-- FAQ ITEM -->
    <div class="faq-item rounded-2xl md:rounded-3xl border border-black/10 bg-white/80 backdrop-blur-xl p-4 md:p-6">
      <button class="faq-summary flex w-full cursor-pointer items-center justify-between text-base md:text-lg font-semibold text-gray-800">
        Is my donation secure?
        <span class="ml-4 flex items-center justify-center text-gray-400 transition-transform duration-300">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M6 9l6 6 6-6" />
          </svg>
        </span>
      </button>
      <div class="faq-content overflow-hidden max-h-0 transition-[max-height] duration-500 ease-in-out">
        <p class="mt-4 text-gray-600 leading-relaxed">
          Yes. All online giving platforms we use are secure and encrypted.
          We partner only with trusted payment providers and do not store your
          card or banking details on our systems.
        </p>
      </div>
    </div>

    <!-- FAQ ITEM -->
    <div class="faq-item rounded-3xl border border-black/10 bg-white/80 backdrop-blur-xl p-6">
      <button class="faq-summary flex w-full cursor-pointer items-center justify-between text-lg font-semibold text-gray-800">
        Will I receive a confirmation or receipt?
        <span class="ml-4 flex items-center justify-center text-gray-400 transition-transform duration-300">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M6 9l6 6 6-6" />
          </svg>
        </span>
      </button>
      <div class="faq-content overflow-hidden max-h-0 transition-[max-height] duration-500 ease-in-out">
        <p class="mt-4 text-gray-600 leading-relaxed">
          Yes. For online giving, you’ll receive a confirmation from the payment provider.
          If you require a formal receipt or confirmation, you’re welcome to contact us at
          <a href={`mailto:${supportEmail}`}
             class="font-semibold text-brand-red hover:underline">
            {supportEmail}
          </a>.
        </p>
      </div>
    </div>

    <!-- FAQ ITEM -->
    <div class="faq-item rounded-3xl border border-black/10 bg-white/80 backdrop-blur-xl p-6">
      <button class="faq-summary flex w-full cursor-pointer items-center justify-between text-lg font-semibold text-gray-800">
        Can I give anonymously?
        <span class="ml-4 flex items-center justify-center text-gray-400 transition-transform duration-300">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M6 9l6 6 6-6" />
          </svg>
        </span>
      </button>
      <div class="faq-content overflow-hidden max-h-0 transition-[max-height] duration-500 ease-in-out">
        <p class="mt-4 text-gray-600 leading-relaxed">
          Yes. You’re welcome to give anonymously.
          Our heart is not to track giving, but to steward it faithfully and responsibly.
        </p>
      </div>
    </div>

    <!-- FAQ ITEM -->
    <div class="faq-item rounded-3xl border border-black/10 bg-white/80 backdrop-blur-xl p-6">
      <button class="faq-summary flex w-full cursor-pointer items-center justify-between text-lg font-semibold text-gray-800">
        Can I give if I live outside South Africa?
        <span class="ml-4 flex items-center justify-center text-gray-400 transition-transform duration-300">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M6 9l6 6 6-6" />
          </svg>
        </span>
      </button>
      <div class="faq-content overflow-hidden max-h-0 transition-[max-height] duration-500 ease-in-out">
        <p class="mt-4 text-gray-600 leading-relaxed">
          Yes. If you live outside South Africa, you can give securely through PayPal.
          You can also begin a Western Union transfer through their website.
          If you need recipient details or reference guidance before completing it, contact us.
        </p>
        <p class="mt-3">
          <a
            href={paypal.donateUrl}
            target="_blank"
            rel="noopener"
            class="inline-flex items-center gap-2 font-semibold text-brand-red hover:underline"
          >
            Click here to give via PayPal →
          </a>
        </p>
        <p class="mt-3">
          <a
            href={westernUnion.url}
            target="_blank"
            rel="noopener"
            class="inline-flex items-center gap-2 font-semibold text-brand-red hover:underline"
          >
            Start a Western Union transfer →
          </a>
        </p>
      </div>
    </div>

    <!-- FAQ ITEM -->
    <div class="faq-item rounded-3xl border border-black/10 bg-white/80 backdrop-blur-xl p-6">
      <button class="faq-summary flex w-full cursor-pointer items-center justify-between text-lg font-semibold text-gray-800">
        Is my giving tax-deductible?
        <span class="ml-4 flex items-center justify-center text-gray-400 transition-transform duration-300">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M6 9l6 6 6-6" />
          </svg>
        </span>
      </button>
      <div class="faq-content overflow-hidden max-h-0 transition-[max-height] duration-500 ease-in-out">
        <p class="mt-4 text-gray-600 leading-relaxed">
          Tax donation receipts are available where applicable.
          If you have questions about receipts or documentation, please contact us at
          <a href={`mailto:${supportEmail}`}
             class="font-semibold text-brand-red hover:underline">
            {supportEmail}
          </a>.
        </p>
      </div>
    </div>

    <!-- FAQ ITEM -->
    <div class="faq-item rounded-3xl border border-black/10 bg-white/80 backdrop-blur-xl p-6">
      <button class="faq-summary flex w-full cursor-pointer items-center justify-between text-lg font-semibold text-gray-800">
        How are donations used?
        <span class="ml-4 flex items-center justify-center text-gray-400 transition-transform duration-300">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M6 9l6 6 6-6" />
          </svg>
        </span>
      </button>
      <div class="faq-content overflow-hidden max-h-0 transition-[max-height] duration-500 ease-in-out">
        <p class="mt-4 text-gray-600 leading-relaxed">
          Gifts support the ministry, mission, and practical needs of The Life Place —
          including gatherings, pastoral care, community initiatives, and the ongoing
          work of the church.
        </p>
      </div>
    </div>

  </div>

  <!-- Soft footer reassurance -->
  <p class="mt-12 px-4 md:px-0 text-center text-sm md:text-base text-gray-500 break-words">
    Still have questions?
    <a href={`mailto:${supportEmail}`} class="font-semibold text-brand-red hover:underline">
      We’re happy to help.
    </a>
  </p>
</section>

</div>
<!-- Final Invitation -->
<section class="py-24 md:py-32 bg-white">
  <div class="max-w-4xl mx-auto px-6 text-center">
    
    <h2 class="text-3xl md:text-5xl font-black mb-6 text-gray-800">
      Come. See. <span class="text-brand-red">Jesus</span>
    </h2>

    <p class="text-xl text-gray-600 mb-12 max-w-2xl mx-auto">
      Join us this Sunday.
    </p>

  </div>
</section>

</BaseLayout>

<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const items = document.querySelectorAll(".faq-item");

    items.forEach((item, index) => {
      const summary = item.querySelector(".faq-summary");
      const content = item.querySelector(".faq-content");
      const icon = summary.querySelector("svg");

      // ARIA wiring
      const buttonId = `faq-button-${index}`;
      const panelId = `faq-panel-${index}`;
      summary.setAttribute("id", buttonId);
      summary.setAttribute("aria-controls", panelId);
      summary.setAttribute("aria-expanded", "false");
      content.setAttribute("id", panelId);
      content.setAttribute("role", "region");
      content.setAttribute("aria-labelledby", buttonId);

      summary.addEventListener("click", () => {
        const isOpen = content.style.maxHeight && content.style.maxHeight !== "0px";

        // Close all
        items.forEach((other, i) => {
          const otherSummary = other.querySelector(".faq-summary");
          const otherContent = other.querySelector(".faq-content");
          const otherIcon = other.querySelector("svg");
          if (otherContent) otherContent.style.maxHeight = null;
          if (otherIcon) otherIcon.style.transform = "rotate(0deg)";
          if (otherSummary) otherSummary.setAttribute("aria-expanded", "false");
          other.classList.remove("open");
        });

        // Open clicked one
        if (!isOpen) {
          content.style.maxHeight = content.scrollHeight + "px";
          if (icon) icon.style.transform = "rotate(180deg)";
          summary.setAttribute("aria-expanded", "true");
          item.classList.add("open");
        }
      });
    });
  });
</script>

<style>
  /* Method card fade-in animation */

  /* Single hero image crop */
  .hero-single-img {
    object-position: center center;
  }

  @media (max-width: 639.98px) {
    .hero-single-img {
      object-position: 60% center;
    }
  }
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .fadeIn {
    animation: fadeIn 0.4s ease-out forwards;
  }

  /* QuickGive intro text styling */
  .quickgive-intro {
    animation: fadeIn 0.5s ease-out;
  }

  .no-scrollbar::-webkit-scrollbar { display: none; }
  .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }

  /* Hero image crops */
  .giving-hero-left {
    /* Restore previous crop so left hero photo sits correctly */
    object-position: right center;
  }

  .giving-hero-right {
    object-position: center center;
  }

  @media (max-width: 1023.98px) {
    .giving-hero-right {
      /* On mobile + tablet, keep image slightly left of center but further from the edge */
      object-position: 85% center;
    }
  }
</style>
