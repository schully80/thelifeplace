---
// Dev-only route that reuses the hidden `_dev` splash page so you can load it in the browser.
import SplashTest from "../_dev/splash-test.astro";
export const prerender = false;
---

<SplashTest />
