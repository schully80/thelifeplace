---
import BaseLayout from "../../../layouts/BaseLayout.astro";

// Dynamic route disabled safely:
export function getStaticPaths() {
  return []; // 👈 generates no pages = disabled
}

const series = { name: "Coming Soon", messages: [] };
---

<BaseLayout title="Series Coming Soon">
  <section class="mt-16 text-center">
    <h1 class="text-3xl font-bold text-gray-800">Series Coming Soon</h1>
    <p class="text-gray-500 mt-2">Series pages are not active yet.</p>
  </section>
</BaseLayout>
