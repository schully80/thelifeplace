---
const {
  mt = "mt-40",
  mb = "mb-24",
  color = "bg-gray-400",
  width = "w-[95%]",
} = Astro.props;
---
<div class={`w-full ${mt} ${mb}`}>
  <div class={`${width} mx-auto h-[2px] ${color}`}></div>
</div>
