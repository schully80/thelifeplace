---
/* No keys needed — clean minimal validation version */
---

<style>
  /* Floating label wrapper */
  .field {
    position: relative;
  }

  .field input {
    width: 100%;
    border: 1.5px solid #e5e7eb;
    border-radius: 0.75rem;
    padding: 14px 12px;
    font-size: 15px;
    background: rgba(255, 255, 255, 0.7);
    outline: none;
    transition: all .25s ease;
    font-family: inherit;
  }

  .field input:focus {
    border-color: #B3282D;
    background: rgba(255, 255, 255, 0.95);
    box-shadow: 0 0 0 3px rgba(179, 40, 45, 0.1);
  }

  .field label {
    position: absolute;
    left: 12px;
    top: 14px;
    font-size: 15px;
    color: #9ca3af;
    pointer-events: none;
    transition: all .25s ease;
    font-weight: 500;
    origin: left top;
    background: white;
    padding: 0 4px;
  }

  .field input:focus + label,
  .field input:not(:placeholder-shown) + label {
    transform: translateY(-30px) scale(0.85);
    font-size: 12px;
    color: #B3282D;
    font-weight: 600;
    background: white;
  }

  /* Error message */
  .error-text {
    font-size: 13px;
    color: #B3282D;
    margin-top: 6px;
    display: none;
    font-weight: 500;
  }

  .show-error .error-text {
    display: block;
  }

  .show-error .field input {
    border-color: #B3282D;
    background: rgba(179, 40, 45, 0.05);
  }

  /* Shake animation */
  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    20% { transform: translateX(-6px); }
    40% { transform: translateX(6px); }
    60% { transform: translateX(-4px); }
    80% { transform: translateX(4px); }
  }

  .shake {
    animation: shake 0.3s ease-in-out;
  }
</style>

<section class="space-y-12 pb-8">

  <!-- USER INFO -->
  <h2 class="text-xl font-semibold mt-8 text-center">Your Details</h2>


<p class="text-sm text-gray-500 mt-3 mb-6">
  * Required fields
</p>


  <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">



    <div class="field">
      <input id="firstName" name="firstName" type="text" required placeholder=" " />
      <label>First Name *</label>
      <p class="error-text">Required</p>
    </div>

    <div class="field">
      <input id="lastName" name="lastName" type="text" required placeholder=" " />
      <label>Last Name *</label>
      <p class="error-text">Required</p>
    </div>

    <div class="field">
      <input id="contact" name="contact" type="tel" required placeholder=" " />
      <label>Contact Number *</label>
      <p class="error-text">Required</p>
    </div>

    <div class="field">
      <input id="email" name="email" type="email" required placeholder=" " />
      <label>Email Address *</label>
      <p class="error-text">Enter a valid email</p>
    </div>

    <div class="field">
      <input id="attendees" name="attendees" type="number" required placeholder=" " />
      <label>Number of Persons Attending *</label>
      <p class="error-text">Required</p>
    </div>

  </div>

  <!-- ADDRESS -->
  <h2 class="text-xl font-semibold mt-6 text-center">Your Address</h2>

  <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">

    <div class="field">
      <input id="street_number" name="street_number" type="text" required placeholder=" " />
      <label>Street Number *</label>
      <p class="error-text">Required</p>
    </div>

    <div class="field">
      <input id="street_name" name="street_name" type="text" required placeholder=" " />
      <label>Street Name *</label>
      <p class="error-text">Required</p>
    </div>

    <div class="field">
      <input id="suburb" name="suburb" type="text" required placeholder=" " />
      <label>Suburb *</label>
      <p class="error-text">Required</p>
    </div>

    <div class="field">
      <input id="city" name="city" type="text" required placeholder=" " />
      <label>City / Town *</label>
      <p class="error-text">Required</p>
    </div>

    <div class="field">
      <input id="postal_code" name="postal_code" type="text" required placeholder=" " />
      <label>Postal Code *</label>
      <p class="error-text">Required</p>
    </div>

    <div class="field">
      <input id="province" name="province" type="text" required placeholder=" " />
      <label>Province / State *</label>
      <p class="error-text">Required</p>
    </div>

    <div class="field">
      <input id="country" name="country" type="text" required placeholder=" " />
      <label>Country *</label>
      <p class="error-text">Required</p>
    </div>

  </div>
</section>


<!-- MINIMAL VALIDATION SCRIPT -->
<script is:inline>
  document.addEventListener("submit", (e) => {
    const form = e.target;
    const fields = form.querySelectorAll(".field");

    let firstInvalid = null;

    fields.forEach((field) => {
      const input = field.querySelector("input");

      if (input.hasAttribute("required") && !input.value.trim()) {
        field.classList.add("show-error");
        field.classList.add("shake");

        if (!firstInvalid) firstInvalid = input;

        setTimeout(() => field.classList.remove("shake"), 300);
      } else {
        field.classList.remove("show-error");
      }

      // Minimal email validation
      if (input.type === "email" && input.value.trim() !== "" && !input.value.includes("@")) {
        field.classList.add("show-error");
        field.classList.add("shake");
        if (!firstInvalid) firstInvalid = input;
      }
    });

    if (firstInvalid) {
      e.preventDefault();
      firstInvalid.scrollIntoView({ behavior: "smooth", block: "center" });
    }
  });
</script>
